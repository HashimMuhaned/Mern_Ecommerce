{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/ethereal/(.*)",
      "dest": "/index.html"
    }
  ],
  "rewrites": [
    {
      "source": "/ethereal/(.*)",
      "destination": "/index.html"
    }
  ]
}
