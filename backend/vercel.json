{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server.js"
    }
  ],
  "ignoreCommand": "echo 'Ignore node_modules and .env files'",
  "engines": {
    "node": "18.x"
  }
}
